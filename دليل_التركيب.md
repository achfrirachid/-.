# دليل تركيب Cloudflare Worker (خطوة بخطوة)

## الطريقة السهلة: من الداشبورد مباشرة (بلا Terminal)

### 1) خلق R2 Bucket
1. دخل لـ https://dash.cloudflare.com
2. من القائمة الجانبية → **R2 Object Storage**
3. **Create bucket** → سميه مثلا: `attendance-backups`
4. Create

### 2) خلق الـ Worker
1. من القائمة الجانبية → **Workers & Pages**
2. **Create** → **Create Worker**
3. سميه مثلا: `attendance-backup`
4. Deploy (غادي يعطيك كود افتراضي فارغ)
5. دوس **Edit code**
6. امسح الكود اللي كاين، ولصق فيه المحتوى ديال ملف `worker.js`
7. **Deploy**

### 3) اربط الـ Worker بـ R2 Bucket
1. رجع لصفحة الـ Worker → **Settings** → **Variables**
2. تحت **R2 Bucket Bindings** → **Add binding**
3. **Variable name**: `BACKUP_BUCKET` (بالضبط هكاك، بحروف كبيرة)
4. **R2 bucket**: اختار `attendance-backups`
5. Save/Deploy

### 4) زيد المفتاح السري
1. فنفس صفحة **Settings** → **Variables**
2. تحت **Environment Variables** → **Add variable**
3. **Variable name**: `CF_SECRET`
4. **Value**: دخل سر طويل عشوائي (مثلا 32 حرف)، خبيه فبلاصة آمنة
5. **مهم**: دوس على **Encrypt** باش يتخبى (Secret)
6. Save/Deploy

### 5) خد الرابط ديال الـ Worker
- غادي تلقاه فأعلى صفحة الـ Worker، شكلو تقريبا:
  `https://attendance-backup.<اسمك>.workers.dev`

## دخل المعلومات فالتطبيق
1. فالتطبيق، مرّ لقسم "☁️ نسخة احتياطية Cloudflare"
2. دوس "🔒 فتح" → دخل الكود السري ديال التطبيق (ماشي CF_SECRET، هادا كود آخر)
3. فـ **رابط Worker**: حط الرابط لي خدات فالخطوة 5
4. فـ **المفتاح**: حط نفس القيمة اللي حطيتي فـ `CF_SECRET`
5. دوس "💾 حفظ معلومات Cloudflare"
6. جرب "☁️ صيفط نسخة دابا لـ Cloudflare"

## ملاحظات مهمة
- **CF_SECRET** خاصو يكون **نفسه بالضبط** فالـ Worker وفالتطبيق، وإلا غادي يرجع خطأ 401 Unauthorized
- الملف `wrangler.toml` اختياري، غير إيلا بغيتي تنشر عبر الـ Terminal (`wrangler deploy`) بدل الداشبورد
- ماتحطش المفتاح السري فأي ملف كود عمومي (GitHub) — خليه غير فـ Cloudflare Variables وفـ localStorage ديال التطبيق
