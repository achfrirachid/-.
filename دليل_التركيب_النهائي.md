# دليل تركيب الـ Worker الموحد (GitHub + Dropbox + Telegram + Cloudflare)

## ⚠️ خطوة أولى وواجبة: بطّل التوكنات القديمة
التوكن ديال GitHub (`ghp_eTWpI...`) وديال Dropbox تبعثو فهاد المحادثة وتحطو
لحظياً فكود عمومي — خاصهم يتبطلو ويتعوضو بجداد قبل ما تكمل:
- GitHub: github.com/settings/tokens → Delete على التوكن القديم → Generate new
- Dropbox: dropbox.com/developers/apps → App ديالك → Revoke على التوكن القديم → Generate جديد

بعد ما تدير التوكنات الجداد، استعملهم فالخطوات تحت (ماشي القدام).

## 1) خلق R2 Bucket (إيلا مازال ماخلقتيهش)
dash.cloudflare.com → R2 Object Storage → Create bucket → اسمه `attendance-backups`

## 2) خلق الـ Worker
Workers & Pages → Create → Create Worker → سميه `backup-proxy` → Deploy
→ Edit code → امسح المحتوى وألصق فيه `worker.js` كاملاً → Deploy

## 3) اربط الـ Worker بـ R2
Settings → Variables → R2 Bucket Bindings → Add binding
Variable name: `BACKUP_BUCKET` → اختار `attendance-backups` → Save

## 4) زيد الأسرار (Environment Variables) — كلها Encrypt/Secret
فنفس صفحة Settings → Variables → Environment Variables → Add variable، لكل واحدة:

| الاسم | القيمة |
|---|---|
| `PROXY_SECRET` | سر طويل عشوائي من عندك (مثلا 32 حرف) — نفسه غادي يتحط فالتطبيق |
| `GH_TOKEN` | التوكن **الجديد** ديال GitHub |
| `GH_REPO` | `achfrirachid/achfrirachid.github.io` |
| `GH_PATH` | `backup.json` |
| `GH_BRANCH` | `main` |
| `DBX_TOKEN` | التوكن **الجديد** ديال Dropbox |
| `DBX_PATH` | `/backup.json` |
| `TG_BOT_TOKEN` | `8997111248:AAGGhL6xMlRv37KYzRSZnv-cotIUphTmUrk` |
| `TG_CHAT_ID` | `5769282082` |

Save/Deploy فالآخر.

## 5) خد رابط الـ Worker
فأعلى صفحة الـ Worker، شكلو تقريبا:
`https://backup-proxy.<اسمك>.workers.dev`

## 6) بدل ثلاث حاجات فـ index.html (فالكود، سطرين قريب من بداية قسم Telegram)
```js
const PROXY_URL = 'https://backup-proxy.<اسمك>.workers.dev';  // الرابط ديالك من خطوة 5
const PROXY_SECRET = '...';  // نفس القيمة اللي حطيتي فـ PROXY_SECRET فـ Cloudflare
```
بدلهم بالقيم الحقيقية ديالك، ديفي الملف، دفعو للريبو.

## بعد هادشي
- Telegram، GitHub، Dropbox، Cloudflare (R2) الأربعة غادي يخدمو **تلقائياً 100%**
  بلا ما يطلب التطبيق أي تدخل من المستخدم.
- ماكاين حتى توكن حقيقي فكود الموقع — كلهم مخبأين فـ Cloudflare Environment Variables.
- المفتاح الوحيد اللي فكود الموقع (`PROXY_SECRET`) ماشي خطير: أقصى شيء يقدر
  يديرو حد لقاه هو يستهلك quota الـ Worker، ماشي يوصل لحساباتك.
